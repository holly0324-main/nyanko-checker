# 新キャラ追加時の更新手順

新しい**非コラボの超激レア**が追加されたときの更新手順です。

## いちばん簡単な方法（推奨）
ChatGPTに、次の情報を送って更新版を作らせます。

- 新キャラ名
- キャラNo.
- 所属するガチャ / シリーズ名
- にゃんこDBの第1形態画像URL（分からなければ「調べて」でOK）
- 新しい季節ガチャなど、既存の分類に無い場合はそのガチャ名

例：

> 新キャラ「○○」、No.999、超激ダイナマイツに追加された。にゃんこ所持チェッカーを更新して。

更新版を受け取ったら、**更新前に所持データをバックアップ**してから、GitHub上の同名ファイルを差し替えます。

## GitHubで差し替えるファイル
通常の「既存シリーズへ新キャラ1体追加」なら、基本は次の2ファイルです。

- `data.js` … キャラ本体と所属シリーズを追加
- `sw.js` … キャッシュ名のバージョンを上げ、iPhone側へ新しいデータを配信

アプリの表示や操作も変えた場合は、変更された `index.html` / `app.js` / `style.css` も同時に差し替えます。

## iPhoneだけでGitHubへ更新ファイルを上げる
1. 念のため、チェッカー右上の ⚙︎ → **バックアップを書き出す** → iCloud Driveへ保存
2. SafariでGitHubの `nyanko-checker` リポジトリを開く
3. **Add file → Upload files**（空リポジトリでない場合は上部の `Add file` メニュー。表示されなければ `…` やデスクトップ表示を利用）
4. 更新版のファイルを選択
5. 同名ファイルの更新としてコミットする
6. **Commit changes**
7. GitHub Pagesの再デプロイが終わるまで少し待つ
8. ホーム画面の「にゃんこ所持」を一度閉じて開き直す
9. まだ古い表示なら、数十秒待ってもう一度開き直す

## 更新後に確認すること
- 設定 ⚙︎ の「アプリ版」「キャラデータ」が新しくなっている
- 新キャラが「全キャラ」に表示される
- 正しいガチャ / シリーズで絞ったときにも表示される
- 以前の所持状況が残っている

## 自分で `data.js` を直接編集する場合
`data.js` には、キャラ一覧 `characters` と、ガチャ/シリーズ一覧 `groups` の両方があります。
新キャラは**両方へ登録**してください。片方だけだと「全キャラにはいるのにガチャ絞り込みで出ない」などのズレが起きます。

キャラ例：

```js
{"id":"999","name":"新キャラ名","image":"https://battlecats-db.imgs-server.com/u999-1.png","groups":["dynamites"],"search":"999 新キャラ名"}
```

さらに、対応するグループの `ids` に `"999"` を追加します。

```js
{"id":"dynamites","name":"超激ダイナマイツ",...,"ids":[...,"999"]}
```

最後に `data.js` の `version` と `snapshot`、`sw.js` の `APP_CACHE` 名を更新します。
**`APP_CACHE` を変えないとホーム画面版が古い `data.js` を使い続けることがあるため、ここは重要です。**

## コラボキャラの場合
このチェッカーの方針上、追加しません。
