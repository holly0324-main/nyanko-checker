# 新キャラ追加・ガチャ更新時の手順（v2.1.0以降）

このチェッカーは **キャラ一覧** と **各ガチャの実排出プール** を分離しています。
ガチャ絞り込みの正本は `data.js` の `groups[].ids` だけです。

## 新しい非コラボ超激レアが追加されたとき
ChatGPTへ次を送るのが一番簡単です。

- 新キャラ名
- キャラNo.（不明なら調べてでOK）
- どのガチャに追加されたか（不明なら調べてでOK）
- 「にゃんこ所持チェッカーを更新して」

例：
> 新キャラ「○○」が超激ダイナマイツに追加された。No.も含めて調べて、にゃんこ所持チェッカーを更新して。

## データを直接直す場合
### 1. `characters` にキャラを1回だけ追加
```js
{
  "id": "999",
  "name": "新キャラ名",
  "image": "https://battlecats-db.imgs-server.com/u999-1.png",
  "search": "999 新キャラ名"
}
```

**キャラ側に `groups` は追加しません。** 二重管理をすると再びズレるためです。

### 2. 実際に排出される各ガチャの `groups[].ids` を更新
通常シリーズの新キャラなら、そのシリーズだけでなく、超ネコ祭・極ネコ祭・超極ネコ祭などへ入るかも実際の最新ラインナップで確認します。

「このキャラはこの系列だから多分入る」という推測ではなく、**現行ガチャの排出キャラ一覧を確認してからIDを追加**します。

### 3. 可変ガチャは固定データにしない
次は開催回ごとに選出キャラが変わるため、常設の固定フィルタとして扱いません。

- 超選抜祭 / 極選抜祭
- DL記念選抜
- 周年セレクション
- その他、その回ごとに選抜内容が変わるガチャ

必要なら開催時点のラインナップを1回分のスナップショットとして追加します。

### 4. バージョン更新
- `data.js` の `version` と `snapshot`
- `sw.js` の `APP_CACHE`

を更新します。`APP_CACHE` を変えないとiPhoneのホーム画面版が古い `data.js` を保持する場合があります。

## 更新後の確認
- 「全キャラ」の総数が想定通り
- 対象キャラが正しいガチャで表示される
- 対象外ガチャには表示されない
- 全 `groups[].ids` が実在する超激レアIDだけを参照している
- 以前の所持状況が残っている

## コラボキャラ
このチェッカーの方針上、追加しません。
